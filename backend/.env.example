# OpenAI API Key (required for full functionality)
OPENAI_API_KEY=your_openai_api_key_here

# CORS Origins (comma-separated list, or "*" for all origins)
# For production, set this to your frontend URL(s), e.g.:
# CORS_ORIGINS=https://your-app.vercel.app,https://www.yourdomain.com
CORS_ORIGINS=*

# Scoring Model (optional, defaults to "gpt-4o")
# Options: "gpt-4o", "gpt-4o-mini", "gpt-4-turbo"
SCORING_MODEL=gpt-4o

# Speech Corpus Directory (optional, defaults to "./app/corpus")
# Used for RAG system in Parliamentary mode
SPEECH_CORPUS_DIR=./app/corpus

# Port (set automatically by Railway, don't change)
# PORT=8000
